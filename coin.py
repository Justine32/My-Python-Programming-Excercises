

coins=[25,10,5,1]
multi_list=[2,'a','ssss is a s',{'a':45},(1,2)]

def num_coins(paisa):
    """
    Find the optimal number of coins and return it
    """
    
    count=0
    for coin in coins:
        print(coin)


print(num_coins(30))